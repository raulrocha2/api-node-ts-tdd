version: 2.1
  jobs:
    test:
      steps:
        - checkout
        - run: echo "Running tests"
        - run: npm install
        - run: npx eslint 'src/**' 
        - run: npm run test:coveralls
    build:
      docker:
        - image: circleci/node:12.16
      steps:
        - checkout
        - run: echo "build project"
        - npm install
        - npm run build

workflows:
  version: 2.1
    test_build:
      jobs:
        - test
        